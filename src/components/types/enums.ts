export enum EngineStatus {
  engineStarted = 'started',
  engineStopped = 'stopped',
  engineDrive = 'drive',
}

export enum Sorting {
  not = 'not sorted',
  asc = 'sorted ascending',
  dsc = 'sorted descending',
}
